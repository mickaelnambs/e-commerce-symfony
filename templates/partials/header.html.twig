<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('app_home') }}">Shoppy</a>
    <button class="navbar-toggler d-lg-none" type="button" data-toggle="collapse" data-target="#collapsibleNavId" aria-controls="collapsibleNavId"
        aria-expanded="false" aria-label="Toggle navigation"></button>
    <div class="collapse navbar-collapse" id="collapsibleNavId">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_home') }}"><i class="fa fa-home"> Accueil</i> <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('product_index') }}"><i class="fa fa-shopping-cart"> Produits</i> <span class="sr-only">(current)</span></a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                        {{ app.user.firstName}}
                    </a>

                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDopdownLink">
                        <a href="{{ path('product_cart') }}" class="dropdown-item">Mon panier</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('account_profile') }}" class="dropdown-item">Modifier mon profil</a>
                        <a href="{{ path('account_password') }}" class="dropdown-item">Modifier mon mot de passe</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('account_logout') }}" class="dropdown-item"><i class="fa fa-power-off"></i> Déconnexion</a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item">
                    <a href="{{ path('account_register') }}" class="nav-link"><i class=" fa fa-sign-in"></i> Inscription</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('account_login') }}" class="nav-link"><i class=" fa fa-lock"></i> Connexion</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>