	<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
		<a class="navbar-brand" href="{{ path('app_home') }}">Shoppy</a>
		<button class="navbar-toggler d-lg-none" type="button" data-toggle="collapse" data-target="#collapsibleNavId" aria-controls="collapsibleNavId" aria-expanded="false" aria-label="Toggle navigation"></button>
		<div class="collapse navbar-collapse" id="collapsibleNavId">
			<ul class="navbar-nav mr-auto">
				<li class="nav-item">
					<a class="nav-link" href="{{ path('admin_dashboard_index') }}">
						<i class="fa fa-dashboard"> Dashboard</i>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('admin_product_index') }}">
						<i class="fa fa-shopping-cart"> Produits</i>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('admin_category_index') }}">
						<i class="fa fa-folder"> Catégories</i>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('admin_account_index') }}">
						<i class="fa fa-users"> Utilisateurs</i>
					</a>
				</li>
			</ul>
			<ul class="navbar-nav ml-auto">
				{% if app.user %}
					<li class="nav-item dropdown">
						<a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
							{{ app.user.firstName}}
						</a>

						<div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDopdownLink">
							<a href="{{ path('account_logout') }}" class="dropdown-item">Déconnexion</a>
						</div>
					</li>
				{% endif %}
			</ul>
		</div>
	</nav>
